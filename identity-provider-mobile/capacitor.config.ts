import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'identity-provider-mobile',
  webDir: 'dist'
};

export default config;
